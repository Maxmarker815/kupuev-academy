/* =====================================================================
   ОБЩИЕ НАСТРОЙКИ — одно место на всю платформу.
   Этот файл подключают и index.html (кабинеты), и course.html (уроки),
   поэтому менять адрес таблицы нужно только здесь.
   ===================================================================== */
const CONFIG = {
  // Пока здесь пусто — платформа работает в ДЕМО-режиме на вымышленных
  // данных прямо в браузере. Когда подключишь Google-таблицу
  // (см. файл НАСТРОЙКА.md), вставь сюда адрес, который выдаст
  // Apps Script, — и платформа сама начнёт писать в таблицу.
  API_URL: '',
  GUIDE_URL: 'course.html',
  SCHOOL_NAME: 'KUPUEV ACADEMY'
};
